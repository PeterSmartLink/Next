name: next
description: "Next — the private OTYA owner intelligence app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.11.1
  file_picker: ^12.3.0
  flutter_riverpod: ^3.4.3
  flutter_secure_storage: ^11.1.1
  go_router: ^18.0.1
  local_auth: ^3.0.2
  pdfrx: ^2.6.1
  webview_flutter: ^4.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
